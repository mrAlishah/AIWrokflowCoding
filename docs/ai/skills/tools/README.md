# Tools Skills Area

Canonical new path:

```text
docs/ai/skills/tools/
```

Reason for compatibility:

Existing repository maintenance and policy-sync skills remain in `docs/ai/skills/` and existing skill names remain callable.

Migration note:

Use this folder as the future home for `tools_*` skills. Do not move or rename existing skills in this phase.
