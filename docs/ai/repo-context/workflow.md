# Repository Workflow

Reusable workflow knowledge for this repository.

## Working Rules

- Follow existing project patterns, naming, and standards.
- Keep changes scoped to the requested task.
- Preserve unrelated user changes.
- Do not modify source code during documentation-only tasks.

## Repo Context Update Workflow

1. Invoke `repo-context-update` explicitly.
2. Inspect only the repository areas needed to refresh reusable knowledge.
3. Update only `docs/ai/repo-context/*`.
4. Keep context concise and stable.
5. Report all changed markdown files.

## Common Workflows

Document repository-specific implementation, review, and verification workflows after explicit inspection.

