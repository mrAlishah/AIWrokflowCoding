version: 3
capabilities:
  test:
    providers:
      - provider_type: local_tool
        identifier: repository_tests
        availability: configured
        deterministic: true
        command: test-command
