Run an implementation loop for STP-8050 using the repository Skills and AGENTS.md instructions.

Parameters:

PBI_ID:
STP-123

Base plan:
docs/ai/pbi/STP-8050/02-implementation-plan.md

Execute all phases in order. For each phase:

1. Implement only that phase.
2. Review the phase changes.
3. Fix any required/blocking issue.
4. Review again after fixes.
5. Do not continue to the next phase while required/blocking issues remain.

Rules:

- Follow existing project patterns, naming, structure, and coding standards.
- Keep changes minimal and scoped to the current phase.
- Do not introduce unrelated refactors, unnecessary renames, or unrelated behavior changes.
- Do not run npm run lint or npx eslint unless I explicitly approve it.
- Read extra source/context only when directly needed for the current phase.

At the end, all phases must be complete and the final review must have no required/blocking issues.

Final summary:

- Completed phases
- Changed files
- Review issues fixed
- Validation performed or skipped
- Remaining risks
