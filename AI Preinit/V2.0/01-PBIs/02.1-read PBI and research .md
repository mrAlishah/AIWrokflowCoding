Use the V2 AI Multi-Agent Coding Operating System.

Task:
Prepare this approved PBI for `pbi-workspace-create`.

Context:
The PBI has already been reviewed and approved.
The attached file is the Approved PBI Package in Markdown format.

Goal:
Read and understand the PBI Definition, inspect only the relevant repo-context needed for initial problem understanding, and decide whether the PBI is clear enough to create a PBI workspace.

Read:

1. AGENTS.md
2. docs/ai/START_HERE.md
3. docs/ai/skills/README.md
4. docs/ai/skills/pbi-workspace-create.skill.md
5. Attached Approved PBI Package
6. Relevant repo-context only if needed for understanding the problem

Rules:

- Do not modify source code.
- Do not create implementation tasks.
- Do not create a PBI workspace until I explicitly approve the next step.
- Do not read the whole repository.
- Do not read all docs/ai.
- Do not run review-phase, implementation-phase, or fix-phase.
- Use markdown files as shared memory only when the selected skill allows it.
- If anything is unclear, incomplete, risky, or missing acceptance criteria, stop and ask focused questions in Persian.
- If everything is clear, summarize readiness and ask for my approval before running `pbi-workspace-create`.

Workflow:

1. Read the attached Approved PBI Package.
2. Identify:
   - PBI ID
   - title
   - problem statement
   - goal
   - acceptance criteria
   - constraints
   - risks
   - required inputs for `pbi-workspace-create`
3. Read relevant repo-context only if it helps understand the PBI scope or risk.
4. Decide whether the PBI is ready for workspace creation.
5. If not ready, ask focused Persian questions.
6. If ready, return the exact `pbi-workspace-create` input and ask for approval to continue.

Expected Output:

- PBI understanding summary
- Extracted PBI fields
- Relevant repo-context consulted, if any
- Gaps or risks found
- Persian questions, if needed
- Ready / Not Ready for `pbi-workspace-create`
- Exact proposed `pbi-workspace-create` parameters
- Ask: “آیا تایید می‌کنید مرحله `pbi-workspace-create` را اجرا کنم؟”
